def parse_message(message):
    """
    Will parse the message into something useful
    :param message: Message from an user
    :return: Returns an array with parsed data
    """
    print(message)
    return message
